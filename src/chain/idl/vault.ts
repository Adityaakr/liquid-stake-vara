// Generated by scripts/gen-clients.sh (cargo sails client-js). Do not edit by hand.
/* eslint-disable */
// @ts-nocheck
import {GearApi, HexString} from "@gear-js/api";
import {ActorId, QueryBuilderWithHeader, TransactionBuilderWithHeader, TypeResolver, ZERO_ADDRESS} from "sails-js";
import {InterfaceId, SailsMessageHeader} from "sails-js/parser";
import {IStructField} from "sails-js/types";

export class Vault {
    private _typeResolver: TypeResolver;
    constructor(
        public api: GearApi,
        private _programId?: `0x${string}`,
    ) {
        this._typeResolver = new TypeResolver([]);
    }

    private get registry() {
        return this._typeResolver.registry;
    }

    public get programId(): HexString {
        if (!this._programId) throw new Error("Program ID is not set");
        return this._programId;
    }

    public newCtorFromCode(code: Uint8Array | Buffer | HexString, underlying: ActorId, name: string, $symbol: string, decimals: number, apy_bps: number, instant_fee_bps: number, unbond_period_secs: number): TransactionBuilderWithHeader<null> {
        const builder = new TransactionBuilderWithHeader<null>(
            this.api,
            this.registry,
            "upload_program",
            SailsMessageHeader.v1(InterfaceId.zero(), 0, 0),
            [underlying, name, $symbol, decimals, apy_bps, instant_fee_bps, unbond_period_secs],
            this._typeResolver.getTypeDeclString({"kind":"tuple","types":["ActorId", "String", "String", "u8", "u32", "u32", "u64"]}),
            this._typeResolver.getTypeDeclString("String"),
            code,
        );
        this._programId = builder.programId;
        return builder;
    }

    public newCtorFromCodeId(codeId: `0x${string}`, underlying: ActorId, name: string, $symbol: string, decimals: number, apy_bps: number, instant_fee_bps: number, unbond_period_secs: number): TransactionBuilderWithHeader<null> {
        const builder = new TransactionBuilderWithHeader<null>(
            this.api,
            this.registry,
            "create_program",
            SailsMessageHeader.v1(InterfaceId.zero(), 0, 0),
            [underlying, name, $symbol, decimals, apy_bps, instant_fee_bps, unbond_period_secs],
            this._typeResolver.getTypeDeclString({"kind":"tuple","types":["ActorId", "String", "String", "u8", "u32", "u32", "u64"]}),
            this._typeResolver.getTypeDeclString("String"),
            codeId,
        );
        this._programId = builder.programId;
        return builder;
    }

    public get vft(): Vft {
        return new Vft(this.api, this.programId, 1);
    }

    public get vault(): VaultService {
        return new VaultService(this.api, this.programId, 2);
    }
}

export type VaultError = { Paused: null } | { ZeroAddress: null } | { ZeroAmount: null } | { BelowMinimum: { min: bigint } } | { InsufficientShares: null } | { InsufficientBalance: null } | { InsufficientAllowance: null } | { Unauthorized: null } | { Overflow: null } | { BadConfig: null } | { UnbondNotFound: null } | { UnbondNotReady: { claimable_at: number } } | { /**
 * The underlying token rejected the transfer (allowance, balance, paused).
 */
TokenRejected: string } | { /**
 * The cross-program call failed to complete (timeout, transport, panic).
 */
TokenCall: string } | { /**
 * The message carried less gas than the async flow needs; nothing was moved.
 */
NotEnoughGas: { required: number } };

export class Vft {
    private _typeResolver: TypeResolver;
    constructor(
        private _api: GearApi,
        private _programId: HexString,
        private _routeIdx: number = 0,
    ) {
        this._typeResolver = new TypeResolver([{"name":"VaultError","kind":"enum","variants":[{"name":"Paused","fields":[],"entry_id":0},{"name":"ZeroAddress","fields":[],"entry_id":0},{"name":"ZeroAmount","fields":[],"entry_id":0},{"name":"BelowMinimum","fields":[{"name":"min","type":"U256"}],"entry_id":0},{"name":"InsufficientShares","fields":[],"entry_id":0},{"name":"InsufficientBalance","fields":[],"entry_id":0},{"name":"InsufficientAllowance","fields":[],"entry_id":0},{"name":"Unauthorized","fields":[],"entry_id":0},{"name":"Overflow","fields":[],"entry_id":0},{"name":"BadConfig","fields":[],"entry_id":0},{"name":"UnbondNotFound","fields":[],"entry_id":0},{"name":"UnbondNotReady","fields":[{"name":"claimable_at","type":"u64"}],"entry_id":0},{"name":"TokenRejected","fields":[{"type":"String"}],"entry_id":0,"docs":["The underlying token rejected the transfer (allowance, balance, paused)."]},{"name":"TokenCall","fields":[{"type":"String"}],"entry_id":0,"docs":["The cross-program call failed to complete (timeout, transport, panic)."]},{"name":"NotEnoughGas","fields":[{"name":"required","type":"u64"}],"entry_id":0,"docs":["The message carried less gas than the async flow needs; nothing was moved."]}]}]);
    }
    private get registry() {
        return this._typeResolver.registry;
    }
    public get interfaceId(): InterfaceId {
        return InterfaceId.from("0x46e25d0a680addcd");
    }
    public allowance(owner: ActorId, spender: ActorId): QueryBuilderWithHeader<bigint> {
        return new QueryBuilderWithHeader<bigint>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 0, this._routeIdx),
            [owner, spender],
            this._typeResolver.getTypeDeclString({"kind":"tuple","types":["ActorId", "ActorId"]}),
            this._typeResolver.getTypeDeclString("U256"),
        );
    }

    public approve(spender: ActorId, value: bigint): TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 1, this._routeIdx),
            [spender, value],
            this._typeResolver.getTypeDeclString({"kind":"tuple","types":["ActorId", "U256"]}),
            this._typeResolver.getTypeDeclString("bool"),
            this._programId,
        );
    }

    public balanceOf(account: ActorId): QueryBuilderWithHeader<bigint> {
        return new QueryBuilderWithHeader<bigint>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 2, this._routeIdx),
            account,
            this._typeResolver.getTypeDeclString("ActorId"),
            this._typeResolver.getTypeDeclString("U256"),
        );
    }

    public decimals(): QueryBuilderWithHeader<number> {
        return new QueryBuilderWithHeader<number>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 3, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString("u8"),
        );
    }

    public name(): QueryBuilderWithHeader<string> {
        return new QueryBuilderWithHeader<string>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 4, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString("String"),
        );
    }

    public $symbol(): QueryBuilderWithHeader<string> {
        return new QueryBuilderWithHeader<string>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 5, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString("String"),
        );
    }

    public totalSupply(): QueryBuilderWithHeader<bigint> {
        return new QueryBuilderWithHeader<bigint>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 6, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString("U256"),
        );
    }

    public transfer(to: ActorId, value: bigint): TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 7, this._routeIdx),
            [to, value],
            this._typeResolver.getTypeDeclString({"kind":"tuple","types":["ActorId", "U256"]}),
            this._typeResolver.getTypeDeclString("bool"),
            this._programId,
        );
    }

    public transferFrom($from: ActorId, to: ActorId, value: bigint): TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 8, this._routeIdx),
            [$from, to, value],
            this._typeResolver.getTypeDeclString({"kind":"tuple","types":["ActorId", "ActorId", "U256"]}),
            this._typeResolver.getTypeDeclString("bool"),
            this._programId,
        );
    }

    public subscribeToApprovalEvent<T = { owner: ActorId; spender: ActorId; value: bigint }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"owner","type":"ActorId"},{"name":"spender","type":"ActorId"},{"name":"value","type":"U256"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 0) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToTransferEvent<T = { $from: ActorId; to: ActorId; value: bigint }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"from","type":"ActorId"},{"name":"to","type":"ActorId"},{"name":"value","type":"U256"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 1) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }
}

export interface Position { shares: bigint; assets: bigint }

export interface RedeemPreview { assets: bigint; fee: bigint; net: bigint }

export interface Unbond { id: number; shares: bigint; assets: bigint; requested_at: number; claimable_at: number }


export interface VaultInfo { underlying: ActorId; admin: ActorId; name: string; $symbol: string; decimals: number; rate: bigint; apy_bps: number; instant_fee_bps: number; unbond_period_secs: number; total_shares: bigint; total_assets: bigint; holdings: bigint; fees_accrued: bigint; unbonding_total: bigint; min_deposit: bigint; paused: boolean; last_accrual_at: number }

export class VaultService {
    private _typeResolver: TypeResolver;
    constructor(
        private _api: GearApi,
        private _programId: HexString,
        private _routeIdx: number = 0,
    ) {
        this._typeResolver = new TypeResolver([{"name":"Position","kind":"struct","fields":[{"name":"shares","type":"U256"},{"name":"assets","type":"U256"}]}, {"name":"RedeemPreview","kind":"struct","fields":[{"name":"assets","type":"U256"},{"name":"fee","type":"U256"},{"name":"net","type":"U256"}]}, {"name":"Unbond","kind":"struct","fields":[{"name":"id","type":"u64"},{"name":"shares","type":"U256"},{"name":"assets","type":"U256"},{"name":"requested_at","type":"u64"},{"name":"claimable_at","type":"u64"}]}, {"name":"VaultError","kind":"enum","variants":[{"name":"Paused","fields":[],"entry_id":0},{"name":"ZeroAddress","fields":[],"entry_id":0},{"name":"ZeroAmount","fields":[],"entry_id":0},{"name":"BelowMinimum","fields":[{"name":"min","type":"U256"}],"entry_id":0},{"name":"InsufficientShares","fields":[],"entry_id":0},{"name":"InsufficientBalance","fields":[],"entry_id":0},{"name":"InsufficientAllowance","fields":[],"entry_id":0},{"name":"Unauthorized","fields":[],"entry_id":0},{"name":"Overflow","fields":[],"entry_id":0},{"name":"BadConfig","fields":[],"entry_id":0},{"name":"UnbondNotFound","fields":[],"entry_id":0},{"name":"UnbondNotReady","fields":[{"name":"claimable_at","type":"u64"}],"entry_id":0},{"name":"TokenRejected","fields":[{"type":"String"}],"entry_id":0,"docs":["The underlying token rejected the transfer (allowance, balance, paused)."]},{"name":"TokenCall","fields":[{"type":"String"}],"entry_id":0,"docs":["The cross-program call failed to complete (timeout, transport, panic)."]},{"name":"NotEnoughGas","fields":[{"name":"required","type":"u64"}],"entry_id":0,"docs":["The message carried less gas than the async flow needs; nothing was moved."]}]}, {"name":"VaultInfo","kind":"struct","fields":[{"name":"underlying","type":"ActorId"},{"name":"admin","type":"ActorId"},{"name":"name","type":"String"},{"name":"symbol","type":"String"},{"name":"decimals","type":"u8"},{"name":"rate","type":"U256","docs":["Assets per share scaled by 1e18, projected to now."]},{"name":"apy_bps","type":"u32"},{"name":"instant_fee_bps","type":"u32"},{"name":"unbond_period_secs","type":"u64"},{"name":"total_shares","type":"U256"},{"name":"total_assets","type":"U256","docs":["Assets owed to share holders at the projected rate."]},{"name":"holdings","type":"U256","docs":["Underlying the vault physically holds."]},{"name":"fees_accrued","type":"U256"},{"name":"unbonding_total","type":"U256"},{"name":"min_deposit","type":"U256"},{"name":"paused","type":"bool"},{"name":"last_accrual_at","type":"u64"}]}]);
    }
    private get registry() {
        return this._typeResolver.registry;
    }
    public get interfaceId(): InterfaceId {
        return InterfaceId.from("0xad38dfb2275efacd");
    }
    public accrue(): TransactionBuilderWithHeader<bigint> {
        return new TransactionBuilderWithHeader<bigint>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 0, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString("U256"),
            this._programId,
        );
    }

    public claim(id: number): TransactionBuilderWithHeader<{ ok: bigint } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: bigint } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 1, this._routeIdx),
            id,
            this._typeResolver.getTypeDeclString("u64"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":["U256",{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public collectFees(to: ActorId): TransactionBuilderWithHeader<{ ok: bigint } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: bigint } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 2, this._routeIdx),
            to,
            this._typeResolver.getTypeDeclString("ActorId"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":["U256",{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public deposit(assets: bigint): TransactionBuilderWithHeader<{ ok: bigint } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: bigint } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 3, this._routeIdx),
            assets,
            this._typeResolver.getTypeDeclString("U256"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":["U256",{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public info(): QueryBuilderWithHeader<VaultInfo> {
        return new QueryBuilderWithHeader<VaultInfo>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 4, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString({"kind":"named","name":"VaultInfo"}),
        );
    }

    public pause(): TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 5, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":["bool",{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public position(account: ActorId): QueryBuilderWithHeader<Position> {
        return new QueryBuilderWithHeader<Position>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 6, this._routeIdx),
            account,
            this._typeResolver.getTypeDeclString("ActorId"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Position"}),
        );
    }

    public previewDeposit(assets: bigint): QueryBuilderWithHeader<bigint> {
        return new QueryBuilderWithHeader<bigint>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 7, this._routeIdx),
            assets,
            this._typeResolver.getTypeDeclString("U256"),
            this._typeResolver.getTypeDeclString("U256"),
        );
    }

    public previewRedeem(shares: bigint): QueryBuilderWithHeader<RedeemPreview> {
        return new QueryBuilderWithHeader<RedeemPreview>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 8, this._routeIdx),
            shares,
            this._typeResolver.getTypeDeclString("U256"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"RedeemPreview"}),
        );
    }

    public rate(): QueryBuilderWithHeader<bigint> {
        return new QueryBuilderWithHeader<bigint>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 9, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString("U256"),
        );
    }

    public redeem(shares: bigint): TransactionBuilderWithHeader<{ ok: RedeemPreview } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: RedeemPreview } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 10, this._routeIdx),
            shares,
            this._typeResolver.getTypeDeclString("U256"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":[{"kind":"named","name":"RedeemPreview"},{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public requestUnbond(shares: bigint): TransactionBuilderWithHeader<{ ok: Unbond } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: Unbond } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 11, this._routeIdx),
            shares,
            this._typeResolver.getTypeDeclString("U256"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":[{"kind":"named","name":"Unbond"},{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public resume(): TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 12, this._routeIdx),
            null,
            null,
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":["bool",{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public setConfig(apy_bps: number, instant_fee_bps: number, unbond_period_secs: number): TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 13, this._routeIdx),
            [apy_bps, instant_fee_bps, unbond_period_secs],
            this._typeResolver.getTypeDeclString({"kind":"tuple","types":["u32", "u32", "u64"]}),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":["bool",{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public topUpReserve(assets: bigint): TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 14, this._routeIdx),
            assets,
            this._typeResolver.getTypeDeclString("U256"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":["bool",{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public transferAdmin(to: ActorId): TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }> {
        return new TransactionBuilderWithHeader<{ ok: boolean } | { err: VaultError }>(
            this._api,
            this.registry,
            "send_message",
            SailsMessageHeader.v1(this.interfaceId, 15, this._routeIdx),
            to,
            this._typeResolver.getTypeDeclString("ActorId"),
            this._typeResolver.getTypeDeclString({"kind":"named","name":"Result","generics":["bool",{"kind":"named","name":"VaultError"}]}),
            this._programId,
        );
    }

    public unbonds(account: ActorId): QueryBuilderWithHeader<Unbond[]> {
        return new QueryBuilderWithHeader<Unbond[]>(
            this._api,
            this.registry,
            this._programId,
            SailsMessageHeader.v1(this.interfaceId, 16, this._routeIdx),
            account,
            this._typeResolver.getTypeDeclString("ActorId"),
            this._typeResolver.getTypeDeclString({"kind":"slice","item":{"kind":"named","name":"Unbond"}}),
        );
    }

    public subscribeToAccruedEvent<T = { rate: bigint; total_assets: bigint }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"rate","type":"U256"},{"name":"total_assets","type":"U256"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 0) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToAdminTransferredEvent<T = { $from: ActorId; to: ActorId }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"from","type":"ActorId"},{"name":"to","type":"ActorId"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 1) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToClaimedEvent<T = { owner: ActorId; id: number; assets: bigint }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"owner","type":"ActorId"},{"name":"id","type":"u64"},{"name":"assets","type":"U256"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 2) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToConfigChangedEvent<T = { apy_bps: number; instant_fee_bps: number; unbond_period_secs: number }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"apy_bps","type":"u32"},{"name":"instant_fee_bps","type":"u32"},{"name":"unbond_period_secs","type":"u64"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 3) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToDepositedEvent<T = { owner: ActorId; assets: bigint; shares: bigint; rate: bigint }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"owner","type":"ActorId"},{"name":"assets","type":"U256"},{"name":"shares","type":"U256"},{"name":"rate","type":"U256"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 4) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToFeesCollectedEvent<T = { to: ActorId; assets: bigint }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"to","type":"ActorId"},{"name":"assets","type":"U256"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 5) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToPausedEvent<T = { by: ActorId }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"by","type":"ActorId"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 6) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToRedeemedEvent<T = { owner: ActorId; shares: bigint; assets: bigint; fee: bigint; rate: bigint }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"owner","type":"ActorId"},{"name":"shares","type":"U256"},{"name":"assets","type":"U256"},{"name":"fee","type":"U256"},{"name":"rate","type":"U256"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 7) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToReserveToppedUpEvent<T = { assets: bigint }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"assets","type":"U256"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 8) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToResumedEvent<T = { by: ActorId }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"by","type":"ActorId"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 9) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }

    public subscribeToUnbondRequestedEvent<T = { owner: ActorId; id: number; shares: bigint; assets: bigint; claimable_at: number }>(callback: (eventData: T) => void | Promise<void>): Promise<() => void> {
        const interfaceIdu64 = this.interfaceId.asU64();
        const eventFields = {"fields":[{"name":"owner","type":"ActorId"},{"name":"id","type":"u64"},{"name":"shares","type":"U256"},{"name":"assets","type":"U256"},{"name":"claimable_at","type":"u64"}]}.fields as IStructField[];
        const typeStr = this._typeResolver.getStructDef(eventFields, {}, true);
        return this._api.gearEvents.subscribeToGearEvent("UserMessageSent", ({ data: { message } }) => {
            if (!message.source.eq(this._programId)) return;
            if (!message.destination.eq(ZERO_ADDRESS)) return;

            const { ok, header } = SailsMessageHeader.tryFromBytes(message.payload);
            if (ok && header.interfaceId.asU64() === interfaceIdu64 && header.entryId === 10) {
                callback(this.registry.createType(`([u8; 16], ${typeStr})`, message.payload)[1].toJSON() as T);
            }
        });
    }
}
